git clone <url>

git status ----- to check it don't show any error ass we on't do any change

touch <filename>------create new file

git status-----it will show thw new file in red color---------------that is workspace

git add <filename>
git status ---------------------it will show the file -----this is staging area

git commit -m "description"--------it will throw path error

git config --global user.mail <mailid>
git config --global user.name <username>-----------we have to set the mail and user name

git commit -m "des"-----it will be successful---------------tis is local repo

git push----------this is to remote repo--it will ask for github login-if it is successful file will be there in remote repo

If someone else is having our repo unless we don't give access they can't push it to remote repo but they can download it to local repo